package com.vendor_service.config;

// CORS is configured in SecurityConfig via corsConfigurationSource() bean.
// This file is intentionally a stub to avoid duplicate bean conflicts.
public class CorsConfig {
}